package de.samply.share.broker.model;

/**
 * An enum with a coarse categorization of projects that correspond with the categories on the page.
 */
public enum EnumProjectType {
  PT_NEW,
  PT_OPEN,
  PT_ACTIVE,
  PT_ARCHIVED
}
